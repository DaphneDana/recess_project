REACT

1. npm install -g create-react-app
2. npm list -g
3. npx create-react-app stratcom
4. cd strat
5. npm start‎


useState
useContext
 
⁭
routing pages
fetching data from apis

back end in django
--- installing django --
python -m venv env
source env/bin/activate  # On Windows use `env\Scripts\activate`
pip install django djangorestframework
--- creating project --
django-admin startproject myproject
cd myproject
django-admin startapp myapp
--configuring django rest framework ----
INSTALLED_APPS = [
    ...
    'rest_framework',
    'myapp',
]

creatre modle serialisers



python -m venv venv
>cd venv
cd scripts
activate
"http://127.0.0.1:8000"zScripts>cd../..

pip install djangorestframework pillow django-cors-headers
pip freeze > requirements.txt

django-admin startproject api
cd api
python manage.py startapp default

python manage.py createsuperuser


import React, {useEffect, useState} from "react";
import "../css/low.css";
import axios from 'axios'

grid overflow-hidden transition-all duration-300 ease-in-out text-[#343434]

